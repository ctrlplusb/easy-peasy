diff a/src/create-store-internals.js b/src/create-store-internals.js	(rejected hunks)
@@ -45 +45 @@ export default function createStoreInternals({
-    const draft = createDraft(state);
+      const draft = createDraft(state);
@@ -55 +55 @@ export default function createStoreInternals({
-    const current = get(path, draft);
+      const current = get(path, draft);
@@ -61,2 +61,2 @@ export default function createStoreInternals({
-    return finishDraft(draft);
-  }
+      return finishDraft(draft);
+    }
