name: Setup Node.js
description: A composite action to setup the required Node.js version.

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 22.x
        cache: 'yarn'
